apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-hello-service.fullname" . }}-test-connection"
  labels:
    {{- include "helm-hello-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-hello-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
